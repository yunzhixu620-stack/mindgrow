# Historical plans

Files in this directory are historical iteration records and may describe retired SQLite, Cloudflare Worker or direct-Supabase architectures. They are retained only for product history.

Use the current documents in the parent `docs/` directory, the root `README.md`, `fc-proxy/index.js`, and `supabase-schema.sql` as the source of truth.

